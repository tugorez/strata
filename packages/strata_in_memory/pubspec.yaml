name: strata_in_memory
description: In-memory adapter for the Strata data mapping library, perfect for testing
version: 0.1.0
publish_to: none  # TODO: Remove before publishing
repository: https://github.com/tugorez/strata
issue_tracker: https://github.com/tugorez/strata/issues

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  strata:
    path: ../strata

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
